name: Apply BladeRGB 1.2.0

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/apply-v1.2.0.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Apply BladeRGB 1.2.0 patch
        run: python tools/apply_v1_2_0.py
      - name: Validate Python
        run: python -m compileall -q .
      - name: Validate QML
        run: python qml_sanity.py
      - name: Commit release
        run: |
          rm -f tools/apply_v1_2_0.py
          rm -f .github/workflows/apply-v1.2.0.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: BladeRGB 1.2.0 Effects Lab"
          git push
